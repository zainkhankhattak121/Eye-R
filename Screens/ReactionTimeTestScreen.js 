import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const ReactionTimeTestScreen = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Reaction Time Test</Text>
      <Text style={styles.description}>This is where the Reaction Time Test will go.</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20,
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
  },
  description: {
    fontSize: 18,
    marginTop: 10,
  },
});

export default ReactionTimeTestScreen;
